<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FoodieGram</title>
    <link rel="icon"
        href="images/icons/icono.ico"
        type="image/x-icon">
    <link rel="stylesheet" href="/stylesheets/searchResult.css">
    <link rel="stylesheet" href="/stylesheets/header.css">
    <link rel="stylesheet" href="/stylesheets/footer.css">
    <link rel="stylesheet" href="/stylesheets/general.css">

    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Fjalla+One&family=Kumbh+Sans:wght@300;700&family=Poppins:wght@300&family=Questrial&display=swap" rel="stylesheet">
    
    <!-- UiKit -->
    <!-- <link rel="stylesheet" href="css/uikit.min.css">
    <script src="js/uikit-icons.min.js"></script>
    <script src="js/uikit.min.js"></script> -->
    <!-- UiKit -->

    <!-- UIkit CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/uikit@3.5.5/dist/css/uikit.min.css" />

    <!-- UIkit JS -->
    <script src="https://cdn.jsdelivr.net/npm/uikit@3.5.5/dist/js/uikit.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/uikit@3.5.5/dist/js/uikit-icons.min.js"></script>
</head>

<body>
    <%- include('./partials/header')%>
    <main>
        <div  class="uk-section uk-section-small" >
            <div class="uk-container uk-container-large uk-flex uk-flex-middle uk-flex-center">
                <div >
                    <section>
                        <h2>Búsqueda de usuarios</h2> 
                        <ul id="section1">
                        <% if (resultados.length>0) { %>
                            <% for (let i=0; i< resultados.length; i++){ %>
                               <div>
                                    <label><a class="usuarios" href="/user/detail/<%= resultados[i].id %>">@<%= resultados[i].name %></a></label>
                                </div>
                            <% } %>
                        </ul>
                        <% } else { %>
                            <h2>No se encontraron usuarios para tu busqueda</h2>
                        <% } %>
                    </section>  
                        

                    <section >
                        <h2>Búsqueda avanzada</h2>
                        <ul id="section2">
                            <% if (resultadosPosteos.length>0) { %>
                                <% for (let i=0; i< resultadosPosteos.length; i++){ %>
                                    <li>                                            
                                        <a class="usuarios" href="/post/detail/<%= resultadosPosteos[i].id %>"><img src="<%= resultadosPosteos[i].image_user %>" alt=""></a>
                                        <label><a class="usuarios" href="/user/detail/<%= resultadosPosteos[i].id_user %>">@<%= resultadosPosteos[i].user.name %></a></label>
                                    </li>
                                <% } %>
                        </ul>
                            <% } else { %>
                                <h2>No se encontraron resultados para tu busqueda</h2>
                            <% } %>
                        </section>      


                    </div>
                    
                </div>
            </div>
            


            <div >
                <div class="uk-container uk-container-large uk-flex uk-flex-middle uk-flex-center">       
                    
                </div>
            </div>

        </div>
        
    </main>

    <%- include('./partials/footer') %> 

</body>

</html>


